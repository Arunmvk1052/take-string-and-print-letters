def most_frequent(str1):
     print(str1)    
     count={}
     for x in str1:
          if x in count.keys():
               count[x]+=1
          else:
               count[x]=1
     return count  
print(most_frequent("Missippi"))

